# URL del backend API
# En desarrollo local: apunta al backend corriendo en puerto 3000
# En produccion (Digital Ocean): NO es necesario configurar esto
# porque el frontend se sirve desde el mismo backend (misma URL)
VITE_API_BASE_URL=http://localhost:3000
